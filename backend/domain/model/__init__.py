"""Canonical domain model — the vendor-neutral vocabulary (doc 04). Shared types,
importable by every layer above normalization. May import: backend.platform.
Exempt from module-schema isolation (it is shared vocabulary, not a data module).
"""
