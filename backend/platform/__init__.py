"""Kernel — shared value types (decimal Quantity), identifiers, the AnalyticResult
envelope shape, and lineage types. Owning docs: 02, 04.

May import: (nothing internal). Importable by every layer.
Empty in M1 (Phase 0 guardrails); populated from M2 onward.
"""
